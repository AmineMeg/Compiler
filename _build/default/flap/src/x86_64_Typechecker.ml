# 1 "flap/src/x86-64/x86_64_Typechecker.ml"
(** There is no typechecker for x86-64 programs in flap. *)

type typing_environment = unit

let initial_typing_environment () = ()

let typecheck () _ = ()

let print_typing_environment () = ""
